'use client';

import React from 'react';

import MainpageIcon from '@/features/tagdetail/tagdetail/1';

import MainpageTag from '../tag';

const Tagcom = () => {
  return (
    <div>
      {/* nav */}
      <MainpageIcon />
      {/* layout */}
      <MainpageTag />
    </div>
  );
};

export default Tagcom;
