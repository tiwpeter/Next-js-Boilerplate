'use client';

// src/app/[locale]/(unauth)/tag/page.jsx
import React, { useEffect, useState } from 'react';

const MainpageTag = () => {
 

  return <div>Tag Id page</div>;
};

export default MainpageTag;
{
  /*
  {currentTags.map((tag, index) => (
          <TagsComponent key={index} tag={tag} />
        ))}
  
  */
}
